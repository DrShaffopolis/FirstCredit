{
  "name": "FirstCredit",
  "version": "0.0.1",
  "description": "Proof-of-concept for a credit teaching tool; but mostly just teaching myself Angular 2 and TypeScript",
  "author": "Chris Shaffer <chris@scoutcorpsllc.com>",
  "scripts": {
    "install": "cd client & npm install & cd ../server & npm install",
    "build": "cd client & npm run build & cd ../server & npm run build",
    "run-client-debug": "cd client & npm run build-watch",
    "build-server-debug": "cd server & npm run build-watch",
    "run-server-debug": "cd server & npm run start",
    "start": "cd client & npm run build & cd ../server & npm run start",
    "install-build-start": "npm install & npm run build & npm run start"
  },
  "dependencies": {

  }
}